(()=>{'use strict';const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));const init=root=>{const box=root.querySelector('[data-sc-cpri-index]');const base=root.dataset.apiBase;if(!box||!base)return;fetch(base+'/index?per_page=9',{method:'GET',credentials:'omit',headers:{Accept:'application/json'}}).then(r=>{if(!r.ok)throw new Error('request');return r.json()}).then(data=>{const items=Array.isArray(data.objects)?data.objects:[];if(!items.length){box.innerHTML='<p>No public research records were returned.</p>';return}box.innerHTML='<div class="sc-cpri__grid">'+items.map(item=>'<article class="sc-cpri__record"><small>'+esc(item.type_label||item.type)+'</small><strong>'+esc(item.title)+'</strong><span>'+esc((item.summary||'').slice(0,170))+'</span><a href="'+esc(item.context_url||item.canonical_url||'#')+'">Open public context →</a></article>').join('')+'</div>'}).catch(()=>{box.innerHTML='<p>The public research index is temporarily unavailable. Canonical Library records remain available.</p>'}).finally(()=>box.setAttribute('aria-busy','false'))};document.querySelectorAll('[data-sc-connected-public-research]').forEach(init)})();
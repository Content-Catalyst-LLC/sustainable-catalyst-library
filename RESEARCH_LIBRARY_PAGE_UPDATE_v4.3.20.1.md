# Research Library Page Update — v4.3.20.1

Replace the current Research Library page body with `RESEARCH_LIBRARY_PAGE_v4.3.20.1.html` after deploying the v4.3.20.1 plugin.

The page content is intentionally unchanged from v4.3.20 except for the version marker/class used to scope the Knowledge Pathway alignment repair.

After saving the page:
1. Purge WordPress/page cache.
2. Purge any host/CDN cache if active.
3. Hard-refresh the Research Library page.
4. Confirm the eight question pathways render as four aligned rows of two cards on desktop and one column on mobile.

No Publications page replacement is required.

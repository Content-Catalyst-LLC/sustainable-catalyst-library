# Deploy Library backend v2.30.0

From macOS, copy the backend archive and installer to the VPS, SSH to the server, and execute the installer. The installer backs up the current backend, preserves `.env`, recreates the container, checks health, verifies the 4D terrain contract on a live corpus, validates Platform Core readiness, and reruns hybrid retrieval.

Expected final line:

`PASS: Library backend v2.30.0 4D Knowledge Terrain & Temporal Dynamics deployed.`

# No Backend Redeploy Required — v5.6.0 R3.1

R3.1 changes WordPress presentation and Open Course Finder UI behavior only.

The Python Library backend remains `1.1.0`. PostgreSQL schema/data, Caddy, DNS, port `8087`, API credentials, and the existing indexed corpus are unchanged. No reindex is required for this release.

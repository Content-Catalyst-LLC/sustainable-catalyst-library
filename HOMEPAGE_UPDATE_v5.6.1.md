# Homepage Update — v5.6.1

Recommended insertion:

```text
[sc_library_homepage_console mode="full"]
```

The new console is intended to replace the homepage's older Library-specific presentation if desired, not the entire homepage.

It is deliberately self-contained and should be inserted as a standalone shortcode block/section so the Library plugin controls its responsive layout, console animation, telemetry and controls.

# Research Library page update — v4.3.19

Replace the Research Library page body with `RESEARCH_LIBRARY_PAGE_v4.3.19.html` after the plugin is deployed.

The page retains the v4.3.18 structure. Editorial compression is intentionally deferred until v4.3.19.1.

The v4.3.19 shortcode renders the new My Libraries / Research Libraries interface inside the existing Research Access surface automatically. No separate shortcode is required.

After updating the page:
1. clear WordPress/page cache;
2. clear browser cache;
3. verify anonymous Research Access still searches public providers;
4. sign in with a Sustainable Catalyst / Workspace account and connect a known library;
5. run a Research Access search and confirm `Best legitimate access route` and `Check My Libraries` appear where applicable;
6. verify Publications still renders its full multi-panel deck.

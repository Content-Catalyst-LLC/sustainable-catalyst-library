# Research Library Page Update — v4.3.35

Replace the body of the canonical `/knowledge-libraries/` WordPress page with `RESEARCH_LIBRARY_PAGE_v4.3.35.html`.

## Change

Access Intelligence II is embedded inside the existing Research Access section, after the Public Library Network, rather than adding another large standalone section. The new shortcode is `[sc_access_intelligence_ii title="Access Intelligence II"]`. It ranks access pathways but explicitly does not treat catalog searches, holdings, memberships, or institution login routes as proof of entitlement.

The Publications page does not require replacement.

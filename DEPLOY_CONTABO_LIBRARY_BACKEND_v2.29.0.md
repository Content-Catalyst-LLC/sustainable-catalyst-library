# Deploy Library Backend v2.29.0

Transfer `sustainable-catalyst-library-backend-v2.29.0.zip` and `upgrade_library_backend_v2_29_0_contabo.sh` to `/tmp`, then execute the installer. It preserves `.env`, rebuilds `sc-library-backend`, verifies health, corpus scope, multi-publication analytical fields, Platform Core readiness, and hybrid retrieval.

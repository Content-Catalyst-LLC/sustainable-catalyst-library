# Deploy Knowledge Library backend v2.40.0

Use `upgrade_library_backend_v2_40_0_contabo.sh`. The installer backs up the existing backend, preserves `.env`, rebuilds the Docker service, verifies the 2.40.0 health identity, exercises both temporal lenses and change sets, checks Platform Core readiness, and verifies hybrid retrieval remains online.

# Research Library page update — v4.3.37

Replace the body of the canonical `/knowledge-libraries/` WordPress page with `RESEARCH_LIBRARY_PAGE_v4.3.37.html` after the plugin upgrade.

The update adds a compact **Publications ↔ Research Graph** section after Open Learning II and before the Research Librarian front door. It embeds:

`[sc_publications_research_graph title="Publications ↔ Research Graph"]`

The section is a public graph inspector and an authenticated private-project handoff. It does not expose private research data.

The Publications page itself does not require a replacement page body. The v4.3.37 plugin decorates the existing v4.3.22.4 Field Spotlight model and template conditionally.

# v5.28.0 release bundle

This bundle contains the WordPress plugin, Python backend v2.39.0, complete repository snapshot, release documentation, checksums, Git installer/push script and Contabo backend upgrade script.

The archive is intentionally flat at its root so it can be extracted directly to `~/Downloads/sc-library-v5.28.0-release`.

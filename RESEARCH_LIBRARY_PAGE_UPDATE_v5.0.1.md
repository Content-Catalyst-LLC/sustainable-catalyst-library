# Research Library page update — v5.0.1

Replace the body of the canonical `/knowledge-libraries/` WordPress page with `RESEARCH_LIBRARY_PAGE_v5.0.1.html`.

The change is deliberately compact: no new top-level product section is added. The existing Connected Public Research section now explains the v5.0.1 production-soak/cache hardening, and the existing Research Infrastructure / Production Status surface certifies the ten-scenario soak and safe-route cache boundary.

The Publications page does not require replacement.

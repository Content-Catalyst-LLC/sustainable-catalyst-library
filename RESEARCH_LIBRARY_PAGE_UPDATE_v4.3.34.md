# Research Library Page Update — v4.3.34

Replace the canonical `/knowledge-libraries/` page body with `RESEARCH_LIBRARY_PAGE_v4.3.34.html`.

The page preserves Library ↔ Workspace Continuity and all earlier research surfaces while adding **Metadata Quality & Entity Resolution** between Workspace Continuity and Open Course Finder.

The new embed is `[sc_metadata_quality_center title="Metadata Quality & Entity Resolution"]`. Public visitors see the governance boundary; signed-in editors can inspect individual Research Sources and Named Entities. Quality scores and duplicate/entity candidates are diagnostic only. Entity canonicalization requires explicit reviewer acceptance and does not delete records or rewrite historical assignments.
